---
- name: recargar reglas de auditd
  ansible.builtin.command:
    cmd: augenrules --load
  changed_when: true

- name: reiniciar auditd
  ansible.builtin.command:
    cmd: systemctl kill -s SIGHUP auditd
  changed_when: true
